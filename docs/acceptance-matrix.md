# Wani 功能与设计验收矩阵

本文档记录 Wani 的实现基线。功能证据来自 2026-08-26 对本机 Things 3 的实际界面检查；视觉证据来自桌面 `Wani/Todo Redesign.dc.html` 及其配套图片。

## 基线优先级

1. 界面视觉、布局和状态表现以 `Todo Redesign.dc.html` 为准。
2. 任务管理的功能和操作流程以实际检查到的 Things 3 行为为准。
3. 同步使用用户私有 iCloud/CloudKit，不复刻 Things Cloud。
4. 设计稿没有覆盖的功能沿用同一视觉语言，不复制 Things 3 的专有图像资源。

## 设计系统

| 项目 | 明确基线 | 验收方式 |
| --- | --- | --- |
| 主窗口 | 1180 × 790 设计画布、258 pt 侧栏、双栏布局、12 pt 外窗圆角；顶栏按钮与 `⌘/` 可显示或隐藏侧栏 | Computer Use 检查标准、窄、宽窗口；切换侧栏不改变当前列表 |
| 浅色 | `#F4EEE6` 主背景、`#EFE7DB` 侧栏、`#FAF5ED` 内容面板、`#FFFDF9` 卡片 | 截图逐项比对 |
| 深色 | `#1D1915` 主背景、`#231E1A` 侧栏、`#1E1A16` 内容面板、`#282219` 卡片 | 截图逐项比对 |
| 默认强调色 | `#C05A34`，并提供 Clay、Slate Blue、Sage、Plum | 设置切换后检查全部强调状态 |
| 字体层级 | SF Pro 系统字体；列表项 13.5 pt；页面标题 29 pt；辅助文字 11–13 pt | 主要列表和编辑卡片截图检查 |
| 密度 | Compact、Medium、Roomy 三档，任务行垂直内边距分别为 6、9、13 pt | 设置后检查列表高度变化 |
| 状态 | hover、选中、展开编辑、完成、空列表、遮罩弹窗均有独立表现 | 鼠标与键盘逐项检查 |

## 核心功能

| 功能单元 | 已确认的 Things 3 行为 | Wani 验收标准 |
| --- | --- | --- |
| 智能列表 | 收件箱、今天、计划、随时、某天、日志簿、废纸篓 | 分类、计数、空状态和跳转全部正确 |
| 今天 | 支持普通时段与“今晚”；可按项目或区域分组 | 今天任务与今晚任务分区正确 |
| 计划 | 按日期分组展示未来任务 | 跨月日期、空日期和任务数量正确 |
| 日志簿 | 完成和取消的项目可归档；归档时间可配置；午夜归档前完成项留在原列表，也可用 `⇧⌘Y` 立即记录；明确归档标题则立即记录 | 完成/取消状态可恢复且按日期展示；原列表、搜索定位与待办计数保持一致 |
| 废纸篓 | 删除项进入废纸篓 | 可恢复、永久清理，其他列表不再显示 |
| 区域 | 区域用于按责任范围组织项目和任务 | 新增、编辑、排序、折叠和删除行为正确 |
| 项目 | 项目包含目标、备注、任务和完成进度 | 新增、编辑、排序、移动、完成和进度正确 |
| 标题 | 项目页底部工具栏提供“新建标题”；点击后在任务区整行内联编辑，不常驻占位行；可从选中且尚未分组的待办创建标题；其待办全部关闭后可归档 | 标题分组、排序及任务移动正确；回车保存、Esc 取消；选中待办一并移入新标题；归档标题进入项目底部已记录项并可恢复 |
| 任务编辑 | 标题、备注、检查清单、标签、时间、截止日期、提醒 | 所有字段可编辑；内联文本变化立即更新时间戳并保存、搜索和同步 |
| 完成状态 | 可标记为已完成或已取消，也可撤销 | 列表、计数、项目进度和日志簿同步更新 |
| 时间 | 今天、今晚、指定日期、某天、清除；选中待办后可从底部“时间”或 `⌘S` 打开日期编辑 | 智能列表归类与日期变化正确；未选中待办时工具栏操作禁用 |
| 重复 | 完成后、每天、每周、每月、每年；支持间隔、提醒和截止日期 | 规则持久化，并在触发条件满足时生成下一项 |
| 移动 | 任务可从底部“移动”或 `⇧⌘M` 移到其他项目、标题或脱离项目 | 目标列表、排序和项目计数即时更新；未选中待办时工具栏操作禁用 |
| 标签 | 任务可设置标签，项目页可按标签筛选 | 标签增删、搜索、筛选和同步正确 |
| 搜索 | 快速查找覆盖任务和列表，并显示任务所属位置 | 覆盖标题、备注、标签、项目和区域，结果可定位 |
| 快速录入 | 应用内快捷录入；Things 3 还提供全局快捷键窗口 | 应用内 `N` 与可配置全局快捷键可保存到指定列表 |
| 多选与编辑 | 全选、复制任务、批量移动和批量删除 | 多选操作对每个选中项一致生效 |

## macOS 设置

| 分区 | 设计稿或 Things 3 已确认项目 | 验收标准 |
| --- | --- | --- |
| General | 登录时启动、菜单栏图标、Dock 今日计数、启动列表、截止日期通知、完成项归档时间 | 设置重启后保持，相关系统行为生效 |
| Appearance | 浅色/深色、强调色、侧栏计数、区域分隔线、列表密度 | 每项即时更新并持久化 |
| Quick Entry | 快速录入、搜索、退出快捷键；默认保存位置 | 快捷键冲突可见，录入目标正确 |
| Sync | 私有 iCloud 状态与最近同步信息 | 离线可用，恢复联网后自动双向同步 |

## 数据与同步

- 区域、项目、标题、任务和检查项使用稳定 UUID。
- 排序值、软删除时间、完成/取消时间和重复规则必须进入持久化模型。
- 任务、层级关系、标签、日期、提醒和排序均进入用户私有 CloudKit 数据库。
- 本地数据库是离线工作副本；CloudKit 不可达时不阻塞增删改。
- 模型与同步容器不得依赖 AppKit 视图，以便未来 iOS target 直接复用。
- 签名与双实例同步的实测状态见 [iCloud 验收记录](icloud-validation.md)。

## 自动化与人工验收

- 单元测试覆盖智能列表归类、日期边界、计数、完成/删除恢复、搜索和重复规则。
- UI 测试覆盖新建、编辑、移动、完成、删除、恢复、搜索和设置。
- 每个功能单元先通过 `xcodebuild`，再使用 Computer Use 在 Wani 中执行真实流程。
- 浅色、深色、窄窗口和宽窗口均需与设计稿比较。
- iCloud 必须在两个独立实例或两个同账户目标间验证新增、编辑、完成、删除和恢复。
